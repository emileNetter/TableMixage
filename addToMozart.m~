function [ output_args ] = addToMozart(fileToadd,mozart )
%ADDTOMOZART Summary of this function goes here
%   Detailed explanation goes here
myFile=eval(['load(''' fileToadd '.mat'')']);
file=eval(['myFile.' filename]);
for i= 0:length(file)
    
end

